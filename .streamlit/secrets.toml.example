# Secrets Streamlit Cloud — à recopier dans Manage app → Settings → Secrets
# Ce fichier n'est qu'un exemple, ne contient AUCUN vrai secret.
# Le vrai secrets.toml ne doit jamais être committé (cf. .gitignore).

INPI_API_USER = "votre.email@example.com"
INPI_API_PASS = "votre_mot_de_passe_inpi"
